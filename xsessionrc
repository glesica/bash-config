# .xsessionrc
#
# Customizations for X sessions.

# Add some stuff to the path where dmenu can find it.
export PATH=$HOME/bin:$HOME/local/bin:$PATH

if [[ "$DISPLAY" != "" ]]; then
    setxkbmap -option "ctrl:nocaps"
fi
